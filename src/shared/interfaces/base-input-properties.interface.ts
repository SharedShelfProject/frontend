export interface BaseInputProperties {
  modelValue: string;
  label: string;
  name: string;
  type?: string;
  placeholder?: string;
  autocomplete?: string;
  maxLength?: number;
  errorMessage?: string;
}
